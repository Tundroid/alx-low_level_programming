#include "main.h"
/**
* _puts - prints a string to the stdout
* @str: string to be printed
*/
void _puts(char *str)
{
	int i = 0;

	while (*(s + i) != '\0')
		_putchar(*(s + i++));
	_putchar('\n');
}
