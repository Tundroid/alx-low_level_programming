#include <stdio.h>
#include "main.h"

/**
* _abs - Returns the absolute of an integerChecks for an alphabet letter
* @n: integer param
*
* Return: absolute value of n
*/
int _abs(int n)
{
	return (n < 0 ? n * -1 : n);
}
